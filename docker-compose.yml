version: "3"
services:
  web:
    build:
      context: ./web
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    depends_on:
      - server
    volumes:
      - ./server:/app/server

  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    ports:
      - "50051:50051"
